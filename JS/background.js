// === Background placeholder (intentionally empty) ===
// The main landing page currently uses a static background driven by CSS.
// If you need to re-enable the experimental WebGL blob scene, you can
// restore it from version control or the commented block kept previously.